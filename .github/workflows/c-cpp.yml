name: BUILD / TEST / MEM CHECK / UB CHECK

on:
  push:
    branches: [ master ]

  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: MAKE
      run:
        make test SANITIZE=1
